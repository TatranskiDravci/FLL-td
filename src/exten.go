package main

import (
	"fmt"
)

// func Within(a, b, thresh int) bool {
// 	return b < (a + thresh) && b > (a - thresh)
// }

func AwaitButton() {
	fmt.Scanf("%s")
}

// func ButtonSig(sig *bool) {
// 	fmt.Scanf("%s")
// 	*sig = true
// }
